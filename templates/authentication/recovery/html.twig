{#
 # Passed in variables:
 #   * name                 — string    Display name of the registered user
 #   * email                — string    Email address of the registered user
 #   * link                 — string    Password reset URL
 #   * auth               — array     Auth data:
 #     * guid               — string    GUID (unique ID)
 #     * email              — string    Email address
 #     * displayname        — string    Public display name
 #     * enabled            — boolean   True if auth account is enabled
 #     * verified           — boolean   True if auth account is verified
 #     * roles              — array     Roles assigned to auth
 #}
<p>{{ __('Dear') }} {{ name }},</p>

<p>{{ __('You or someone else has requested to reset your account password on') }} {{ app.config.get('general/branding/name') }}.</p>

<p><strong>{{ __('If this was not you, please delete this email.') }}</strong></p>

<p>{{ __('You can complete the process by') }} <a href="{{ link }}">{{ __('following this link') }}</a>.</p>

