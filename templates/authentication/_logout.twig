{% extends twigparent %}

{% block logout %}
    {{ include('@Members/feedback/feedback.twig') }}

    <div class="logout">
        <div class="row member-logout">
            {{ form_start(form_logout) }}

            {{ form_row(form_logout.logout) }}

            {{ form_end(form_logout) }}
        </div>
    </div>
{% endblock logout %}
