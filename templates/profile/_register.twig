{#
 # Passed in variables:
 #
 #}
{% extends twigparent %}

{% block members %}
    <div class="row">
        <h4>New Member</h4>
    </div>

    <div class="row register">

        <div class="small-12 columns">

            {{ form_start(form_profile) }}
                {{ form_row(form_profile.displayname) }}
            {{ form_row(form_profile.email) }}
            {{ form_row(form_profile.plainPassword.first) }}
            {{ form_row(form_profile.plainPassword.second) }}
                <br>
                {{ form_row(form_profile.submit) }}
            {{ form_end(form_profile) }}

        </div>

    </div>

    {% include 'feedback.twig' %}
{% endblock %}
